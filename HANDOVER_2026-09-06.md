# 真机 FUEL 探索调试 · 交接文档（2026-09-06）

> 目标：把“真机（无深度相机、用 Livox Mid-360 点云）跑 FUEL 探索”时遇到的三个问题的**现状、根因、证据、建议修复、落地状态**一次性交代清楚，方便下次直接接着干。

---

## 0. 一句话现状

- 探索**流程能跑通**（起飞→悬停→自动/手动触发→建图探索→回航降落）。
- 本轮已完成三个问题的**根因定位（有代码 + 录包证据）**，并给出修复方案。
- ⚠️ **所有建议改动目前均未落地**：曾提交的 `exploration_real.launch`、`algorithm.xml` 修改**已被撤销**，恢复原样；C++ 层方案（360°雷达带模型）只有设计、未改代码。下一步先做“是否重新应用”的决策。

---

## 1. 系统架构与启动流程

**传感器/平台**
- 自组四旋翼：PX4 飞控 + MAVROS；机载电脑 arm64 Ubuntu 20.04 / ROS Noetic。
- 定位/建图：Livox Mid-360（**前倾 15° 安装**）→ FAST-LIO → `/Odom_high_freq`（里程计）与 `/cloud_registered`（注册点云）。
- 底层控制：`px4ctrl`（订阅 `/position_cmd`，支持自动起降）。
- 探索：FUEL 全家桶（`exploration_node` / `traj_server` / `waypoint_generator` / `plan_env` 地图）+ 自写 `fuel_bridge`（odom→PoseStamped 适配）。

**启动顺序（根目录脚本）**

| 顺序 | 脚本 / 命令 | 作用 |
|---|---|---|
| 1 | `start_sensor.sh` | mavros + 解锁/校准 + Livox 驱动 |
| 2 | `start_mapping.sh` | FAST-LIO 建图（rviz:=false） |
| 3 | `start_run_ctrl.sh` | px4ctrl（run_ctrl.launch，odom→`/Odom_high_freq`，cmd→`/position_cmd`） |
| 4 | 手动 roslaunch | **`exploration_manager/launch/exploration_real.launch`**（真正实跑的文件；编辑器里一直开的这个） |
| 5 | `start_takeoff.sh` | 发 `/px4ctrl/takeoff_land` cmd=1 → 自动起飞到 1.0 m 悬停 |
| 6 | RViz 操作 | 点 “2D Nav Goal” 触发探索（当前被自动触发问题干扰，见 P2） |
| 7 | `start_land.sh` | cmd=2 → 降落 |

**关键话题**
- `/Odom_high_freq`：FAST-LIO 里程计（200 Hz）
- `/cloud_registered`：注册点云 → FUEL 地图
- `/map_ros/pose`：fuel_bridge 出的 PoseStamped（供地图 ray casting）
- `/position_cmd`：traj_server → px4ctrl
- `/planning/bspline`、`/waypoint_generator/waypoints`、`/move_base_simple/goal`、`/traj_start_trigger`
- `/sdf_map/occupancy_all`、`/sdf_map/occupancy_local_inflate`（可视化/验收）
- `/px4ctrl/takeoff_land`、`/debugPx4ctrl`

**场地/探索范围（exploration_real.launch 内）**
- 场地约 5×6 m（四周有网）；地图 size 20×40×4 m。
- 硬边界 box：x∈[−3,3]，y∈[−3.5,3.5]，z∈[0,2.5]（必须盖住网，网才会被当前沿去飞近建图，否则提前 finish）。
- 测试布障：在机**斜前、斜后各放一个 0.5×0.5×1.1 m 箱子**。

---

## 2. 三个问题与根因（结论 + 证据）

### P1｜开始探索瞬间“嗖”上窜约 0.5 m，又回落，然后才正常探索
- **判断**：大概率是“第一条轨迹的第一个 viewpoint 被选在比悬停高约 0.4~0.5 m 的位置”（FUEL 倾向选能俯视前沿的高视角），飞上去后前沿被覆盖/重规划，回落到悬停高度再正常跑。叠加此前“自动触发”在悬停刚达成 ~2 s、机体可能仍在收敛时就开跑，瞬态被放大。
- **证据**：`e13.bag`（无箱那次）第一条轨迹也从 z≈1.0 爬向 z≈1.44 的 viewpoint；但**未复现**“上去又下来”的尖峰，故未能 100% 定案。
- **待办**：复测时录 `/position_cmd` + `/Odom_high_freq` + `/debugPx4ctrl`，对比“命令 z 先跳”还是“只有实际 z 跳”，即可区分规划选点 vs 控制瞬态。改手动触发、等彻底悬停后再点，应能显著改善。

### P2｜没有在 RViz 里点，悬停完就自动开始探索（根因已坐实）
- **因果链**：
  1. FUEL FSM 平时停在 `WAIT_TRIGGER`，只有收到 `/waypoint_generator/waypoints` 才触发（`fast_exploration_fsm.cpp::triggerCallback`）。
  2. `px4ctrl` 在 `AUTO_TAKEOFF` 到达高度切入 `AUTO_HOVER` 后约 **2 s**（`PX4CtrlFSM.h::DELAY_TRIGGER_TIME = 2.0`）会**自动** `publish_trigger()` → 发 `/traj_start_trigger`（这是 REAL_DRONE 给 ego_planner 的“已悬停可接管”信号）。
  3. 当前 `exploration_real.launch` 把 `waypoint_generator` 的 `~traj_start_trigger` **remap 到全局 `/traj_start_trigger`**，且 `waypoint_type=point` → 收到后自动生成 `/waypoint_generator/waypoints` → FUEL 被触发。
- **证据**：`e13.bag` 中悬停稳定在 t≈15.5 s，第一条 `/position_cmd` 出现在 **t≈17.6 s（≈悬停后 2 s）**，全程无人点击。
- **建议修复（未落地）**：`exploration_real.launch` 中
  - 删掉 `<remap from="~traj_start_trigger" to="/traj_start_trigger"/>`；
  - `waypoint_type` 改为 `manual-lonely-waypoint`（这样 RViz 里任意一次 2D Nav Goal 点击、含地面 z=0，都会发 waypoints 触发；只点一下即开始）。
  - 之后流程：悬停稳定 → RViz 点 2D Nav Goal → 才开始；不点就一直悬停。

### P3｜箱子后方还有大片灰色（仍在 box 内）就报“任务完成”
- **机制**：FUEL 判定“完成”= 找不到任何**可覆盖的 frontier 簇**（`planExploreMotion` 返回 `NO_FRONTIER` → `FINISH`）。一个簇要被当作可探前沿需同时满足：
  1. 已知空闲且邻接未知的体素、高度 ≥0.4 m（滤地面噪点）；
  2. 簇体素数 > `frontier/cluster_min`（现 100，0.1 m 分辨率）；
  3. 能在“簇平均高度、半径 1.5~2.5 m 环”上采到合格视角：在 box 内、不在障碍膨胀区（0.199 m）、不“贴未知”、且按 FOV 模型能看见 ≥ `min_visib_num`（15）个单元；
  4. 采不到视角的簇直接进 dormant，**以后不再访问**。
- 箱子后低矮阴影条带：视角采样高度与簇同高、贴着箱子 → 采样点多落进膨胀区/贴近 unknown 被拒 → 判 dormant 或小于 `cluster_min` 被丢 → 等其余前沿探完，`frontiers_` 变空 → 报完成。**灰色不是 bug 残留，而是 FUEL 认为“已无视角可探”。**
- **可调（均未落地）**：`cluster_min` 100→30~50；`min_visib_num` 15→3~10；`candidate_rmin/rmax` 加大、`dphi` 加密；或提高巡航高度让 Mid-360 越过箱顶。另注意传感器限制：Mid-360 平飞时向下只能看约 −22°，箱后贴地阴影本来就难扫到。

---

## 3. 雷达视场（FOV）适配分析（P3 的姊妹问题）

- FUEL 的 `perception_utils`（`active_perception/`）是**相机式对称视锥**：只有 yaw、无俯仰；垂直 `±top_angle` 对称；水平 `±left/right` 且**每边 <90°**——**永远表达不了 360° + 非对称 + 前倾**的 Mid-360，只能改代码。
- Mid-360 原厂垂直 −7°~+52°；**前倾 15° 后，平飞时等效垂直覆盖 ≈ −22°~+37°**（水平 360°）。
- 关键认知：**地图由真实点云射线更新，FOV 只影响“规划器认为某个视角能覆盖多少前沿单元”**（影响：簇是否被判可覆盖/dormant、选哪个视角），改 FOV 不会污染地图。
- 现状：`top_angle=1.0 rad(≈57°)` 对真实雷达**过于乐观**（向下虚报约 35°）。
- 两个方案（均未落地）：
  - **软方案（不动 C++）**：`min_visib_num`≈3~5、`cluster_min` 下调 → 让真实射线决定覆盖；缺点是可能对个别死角反复白飞。
  - **代码方案（推荐）**：给 `PerceptionUtils` 加“360°雷达带”模式（水平不限、垂直按世界系仰角带 `[−fov_down, +fov_up]` 判断；`use_360/fov_up/fov_down` 参数化，向后兼容 `use_360=false`）。建议 `fov_up=0.646 rad(37°)`、`fov_down=0.384 rad(22°)`（已含 15° 倾角、假设平飞）。launch 里 exploration_node 与 traj_server 两处参数需保持一致。详见上一轮对话的补丁（`perception_utils.h/.cpp` + 两处 launch）。
  - 局限：方案只对齐“能看到什么”，不解决“低矮箱后需要更高/更近视角”的几何限制（需再扩展 `sampleViewpoints` 的多高度采样，改动更大）。

---

## 4. 改动落地状态（务必核对）

| 文件 | 改动内容 | 状态 |
|---|---|---|
| `exploration_manager/launch/exploration_real.launch` | P2 修复：删 `~traj_start_trigger` remap；`waypoint_type`→`manual-lonely-waypoint` | ❌ 已撤销，当前为原样（remap 还在、type=point） |
| `exploration_manager/launch/algorithm.xml` + launch 内 traj_server 块 | FOV `top_angle` 1.0→0.5 rad（折中 −22°~+37°） | ❌ 已撤销，当前 top_angle=1.0 |
| `active_perception/.../perception_utils.{h,cpp}` | 360°雷达带模型（方案，未写） | ⬜ 未落地 |
| `plan_env`（map_ros）| 整幅地图导出 .pcd（`saveMapToPCD()`） | ✅ 已落地（较早之前加的） |

> 快速自检：打开 `exploration_real.launch`，若看到 `waypoint_generator` 节点里仍有 `~traj_start_trigger` remap 且 `waypoint_type=point`，说明 P2 修复没生效/被回退了。

---

## 5. 下一步建议（按优先级）

1. **决策 P2**：是否重新应用“手动触发”修复（推荐做——否则每次都会自动开跑，无法验证其它问题）。
2. **决策 FOV**：先试软方案调参，还是直接上 360°雷达带代码（需要改 `active_perception` 并 `catkin_make --pkg active_perception` 及依赖它的包）。
3. **复测 P1**：手动触发、机体完全稳定后再点，看“上窜 0.5 m”是否消失；同时录 `/position_cmd`、`/Odom_high_freq`、`/debugPx4ctrl` 对比 cmd z 与真实 z。
4. **验证 P3**：用 PCD 导出功能（`rostopic pub -1 /exploration_node/save_map std_msgs/Bool "data: true"`，默认 `$HOME/explored_map.pcd`）验收箱后灰区是否被扫到。
5. 记得 **`e13.bag` 里那次是自动触发 + 无箱** 的运行，分析结论只对 P2 完全适用，P1/P3 需新录带箱数据。

---

## 6. 重要文件与参数速查

- 探索 launch：`src/fuel_planner/exploration_manager/launch/exploration_real.launch`（真机）、`algorithm.xml`（算法参数）、`exploration.launch`（带仿真，勿用于真机）。
- 探索 FSM：`src/fuel_planner/exploration_manager/src/fast_exploration_fsm.cpp`（状态机/触发）。
- 探索规划：`.../exploration_manager/src/fast_exploration_manager.cpp`（`NO_FRONTIER`→FINISH 在这里）。
- 前沿：`src/fuel_planner/active_perception/src/frontier_finder.cpp`（cluster_min/min_visib_num/dormant/覆盖判断）。
- FOV 模型：`src/fuel_planner/active_perception/src/perception_utils.cpp`。
- 起降 FSM：`src/realflight_modules/px4ctrl/src/PX4CtrlFSM.cpp`（自动触发源头）。
- 轨迹服务器：`src/fuel_planner/plan_manage/src/traj_server.cpp`（发布 `/position_cmd`）。
- 关键参数（algorithm.xml）：`sdf_map/*`（分辨率 0.1、膨胀 0.199、box 边界）、`frontier/*`（cluster_min=100、min_visib_num=15、视角采样 1.5~2.5 m）、`perception_utils/*`（top_angle=1.0、left/right=1.55、max_dist=10）、`fsm/thresh_replan*`、`exploration/*`、`manager/max_vel=0.9`。

---

## 7. 注意与坑（写给接手的人）

- ❗ **不要把 `waypoint_generator ~traj_start_trigger` remap 到 `/traj_start_trigger`**：px4ctrl 会自动发，导致“没点就开跑”。
- ❗ FUEL FOV 模型是对称相机锥：无俯仰、垂直对称、水平每边 <90°；配 Mid-360 必须改代码或接受近似。
- ❗ Mid-360 前倾 15° → 平飞等效垂直覆盖 **−22°~+37°**（0.646 / 0.384 rad）。
- ❗ 地图更新只来自真实点云射线；FOV 参数不会污染地图，只影响规划“以为能看到什么”。
- ❗ FAST-LIO 世界系=camera_init，与 FUEL 的 world 用一层 0 平移静态 TF 对齐。
- ❗ `map_ros` 的 saveMapToPCD 只重编 `plan_env` 即可（`catkin_make --pkg plan_env`）。
- 数据分析用 `e13.bag`（根目录，5.2 MB，44 s），临时分析脚本在 `/tmp/analyze_bag.py`（机器重启会丢，需要可重新生成）。

---

*本文件基于 2026-09-06 会话整理；所有“未落地”项在重新应用前，请以当前源码为准。*
